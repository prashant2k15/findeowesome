# Public lists harvested by the github_harvest job.
#
# Any source that returns raw text/markdown/CSV can be used: raw GitHub files,
# gists, or a controlled hosted list. `kind` is only a hint; the live checker
# re-classifies every discovered URL from the actual page.
#
# The github_discover job automatically adds new public list files it finds.
# Keep this file empty by default so a fresh deployment does not waste a run on
# a known-invalid placeholder URL.

sources: []
